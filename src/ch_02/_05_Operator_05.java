package ch_02;

public class _05_Operator_05 {
    public static void main(String[] args) {

        //삼항 연산자
//  결과(대입될 변수) = 조건 ? true 결과값 : false 결과값
        int x = 10;
        int y = 5;
        int max = (x > y) ? x : y;
        // * 삼항연산자 대신 Math.max(x,y) 함수 사용도이 가능함

    }
}
